version: "3.9"

services:
  postgres:
    image: postgres:16
    container_name: hcp_crm_postgres
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: hcp_crm
    ports:
      - "5432:5432"
    volumes:
      - hcp_crm_postgres_data:/var/lib/postgresql/data

volumes:
  hcp_crm_postgres_data:
